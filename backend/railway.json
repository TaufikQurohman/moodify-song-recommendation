{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "python scripts/download_model.py"
  },
  "deploy": {
    "startCommand": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
